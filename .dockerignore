# L'image RunPod ne contient que le serveur du pod (pod_server.py +
# requirements.txt).
# Tout le reste est du contexte inutile à envoyer au démon Docker.
app/
tests/
data/
.venv/
.git/
.github/
*.md
*.bat
*.sh
run.py
requirements-app.txt
requirements-dev.txt
pytest.ini
__pycache__/
*.pyc
