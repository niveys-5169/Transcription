# Dépendances de l'application locale (serveur web + traitement).
# Le worker RunPod a ses propres dépendances, épinglées dans requirements.txt.

# Serveur web
fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9

# Traitement audio et appels réseau
numpy>=1.24
httpx>=0.27
imageio-ffmpeg>=0.5          # ffmpeg embarqué, si absent du système

# Transcription sur cette machine
faster-whisper>=1.0
# Backend de transfert accéléré de huggingface_hub (téléchargement des
# modèles Whisper) : sans lui, HF_XET_HIGH_PERFORMANCE (voir
# app/engines/local.py) n'a aucun effet et on retombe sur un simple GET HTTP.
hf_xet>=1.0

# Relecture par Claude, uniquement pour le back-end "api" (clé API Anthropic).
# Le back-end par défaut ("cli", sur l'abonnement) appelle le binaire claude
# en sous-processus et n'a pas besoin de ce paquet. Facultatif dans les deux
# cas : sans back-end disponible, repli sur la relecture simple.
anthropic>=0.69

# Synchronisation NotebookLM (étape 5, facultative) : compile les cours
# relus dans un Google Doc maître sur Drive. Sans ces paquets, l'étape se
# désactive toute seule avec un message clair au lieu de faire échouer le
# reste du pipeline — voir app/notebooklm_sync.py.
google-api-python-client>=2.130
google-auth-oauthlib>=1.2
google-auth-httplib2>=0.2
